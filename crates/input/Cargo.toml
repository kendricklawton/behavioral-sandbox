[package]
name = "bsx-input"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
authors.workspace = true
publish = false
description = "The guest's keyboard and pointer: the devices, their reports, and the line grammar every feeder speaks."

[dependencies]
# The device builders and the event type the senders take.
bsx-krun = { path = "../krun" }

[lints]
workspace = true
